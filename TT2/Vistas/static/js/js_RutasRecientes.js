document.addEventListener("DOMContentLoaded", function() {
    // Botón para generar nueva ruta
    document.getElementById("generarNuevaRuta").addEventListener("click", () => {
        window.location.href = '/';
    });
    
    // Aquí se implementaría la lógica para cargar rutas guardadas del usuario
    // Por ahora, solo mostramos las rutas de ejemplo del HTML
});
